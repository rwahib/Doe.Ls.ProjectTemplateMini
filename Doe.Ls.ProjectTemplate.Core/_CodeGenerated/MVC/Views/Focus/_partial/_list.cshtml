 
 



@model IEnumerable<Focus>
@{    
    ViewBagWrapper.InfoBag.SetTitle("Focus List",ViewData);
    var user = ViewBagWrapper.UserInfoExtensionBag.GetCurrentUser(ViewData);
    var task = ViewBagWrapper.TaskBag.GetCurrentTask(ViewData);
}


        <div class="table-responsive">
            <table class="table table-striped table-condensed dt-responsive @*ajaxDataTable*@" @*data-service="focusService" data-url="@Url.Action("ListJson")"*@>
                @*<caption>Focus list</caption>*@
                <thead>
                    <tr>
                     <th>
                            @Html.DisplayNameFor(model => model.FocusId)
                        </th>
                                 
                     <th>
                            @Html.DisplayNameFor(model => model.FocusName)
                        </th>
                                 
                     <th>
                            @Html.DisplayNameFor(model => model.OrderList)
                        </th>
                                 
                        <th class="all"></th>
                    </tr>
                </thead>
                <tbody>
                    @* For ajax dataTable empty the tbody *@
                    @foreach (var item in Model) 
                    {
                       <tr>
                            <td>
   
                                @Html.DisplayFor(modelItem => item.FocusId)
                            </td>                       
                            <td>
   
                                @Html.DisplayFor(modelItem => item.FocusName)
                            </td>                       
                            <td>
   
                                @Html.DisplayFor(modelItem => item.OrderList)
                            </td>                       
                            <td>
                                <div class="btn-group-vertical">
                                    <a href="@Url.Action("Edit", new { id = item.FocusId })" class="btn btn-primary pop-up"  data-toggle="modal" data-target="#vlePluginModal">Edit</a>
                                    <a href="@Url.Action("Details", new { id = item.FocusId })" class="btn btn-primary pop-up"  data-toggle="modal" data-target="#vlePluginModal">Details</a>
                                    <a href="@Url.Action("Delete", new { id = item.FocusId })" class="btn btn-primary pop-up"  data-toggle="modal" data-target="#vlePluginModal">Delete</a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

