 
 



@model IEnumerable<PositionType>
@{    
    ViewBagWrapper.InfoBag.SetTitle("Position type List",ViewData);
    var user = ViewBagWrapper.UserInfoExtensionBag.GetCurrentUser(ViewData);
    var task = ViewBagWrapper.TaskBag.GetCurrentTask(ViewData);
}


        <div class="table-responsive">
            <table class="table table-striped table-condensed dt-responsive @*ajaxDataTable*@" @*data-service="positionTypeService" data-url="@Url.Action("ListJson")"*@>
                @*<caption>Position type list</caption>*@
                <thead>
                    <tr>
                     <th>
                            @Html.DisplayNameFor(model => model.PositionTypeCode)
                        </th>
                                 
                     <th>
                            @Html.DisplayNameFor(model => model.PositionTypeName)
                        </th>
                                 
                     <th>
                            @Html.DisplayNameFor(model => model.PositionTypeDescription)
                        </th>
                                 
                        <th class="all"></th>
                    </tr>
                </thead>
                <tbody>
                    @* For ajax dataTable empty the tbody *@
                    @foreach (var item in Model) 
                    {
                       <tr>
                            <td>
   
                                @Html.DisplayFor(modelItem => item.PositionTypeCode)
                            </td>                       
                            <td>
   
                                @Html.DisplayFor(modelItem => item.PositionTypeName)
                            </td>                       
                            <td>
   
                                @Html.DisplayFor(modelItem => item.PositionTypeDescription)
                            </td>                       
                            <td>
                                <div class="btn-group-vertical">
                                    <a href="@Url.Action("Edit", new { positionTypeCode = item.PositionTypeCode })" class="btn btn-primary pop-up"  data-toggle="modal" data-target="#vlePluginModal">Edit</a>
                                    <a href="@Url.Action("Details", new { positionTypeCode = item.PositionTypeCode })" class="btn btn-primary pop-up"  data-toggle="modal" data-target="#vlePluginModal">Details</a>
                                    <a href="@Url.Action("Delete", new { positionTypeCode = item.PositionTypeCode })" class="btn btn-primary pop-up"  data-toggle="modal" data-target="#vlePluginModal">Delete</a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

