@using Newtonsoft.Json

@{
    ViewBagWrapper.InfoBag.SetTitle("Organisational chart Preview", ViewData);
    var user = ViewBagWrapper.UserInfoExtensionBag.GetCurrentUser(ViewData);
    var loadPreview = "true";
    var posList = ViewBag.PositionList;
    Layout = "~/Views/Shared/_Layout-chart.cshtml";

}
<script type="text/javascript">
    var posListJsonObj = @Html.Raw(JsonConvert.SerializeObject(posList));
</script>
<input type="hidden" id="LoadPreview" value="@loadPreview">

<div class="row col-lg-12 col-lg-offset-3" id="bwnldBtnDiv">
    
    <div class="row col-lg-1 "></div>
    <div class="row ">
        <a href="#" id="download-pdf" class="btn btn-primary none">Download PDF</a>
       
    </div>
</div>
<div class="row col-lg-12">

    <div class="row col-lg-2 "></div>
    <label id="previewHeader"></label>
</div>


 @Html.Partial("_partial/_positionChart")















